using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PartiTest : MonoBehaviour
{
    public ParticleSystem system;

    // Start is called before the first frame update
    void Start()
    {
      // system.Play(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
